## Inline CSS
- Ưu điểm: nhanh chóng, tiện lợi, dễ áp dụng cho phần tử cụ thể, không cần file ngoài.
- Nhược điểm: khó chỉnh sửa, cần rà tìm trong file html dài, dễ làm rối file html.
- Sử dụng khi: test nhanh, ít chỉnh sửa, code ngắn tạm thời.

## Internal CSS
- Ưu điểm: dễ quản lý trong file html, không cần file ngoài.
- Nhược điểm: không thể tái sử dụng, làm dài file html.
- Sử dụng khi: không muốn tách file riêng, dự án nhỏ không cần tái sử dụng css nhiều lần.

## External CSS
- Ưu điểm: có thể tái sử dụng, dễ quản lý trong dự án.
- Nhược điểm: cần quản lý nhiều file.
- Sử dụng khi: dự án phức tạp, dùng định dạng cho nhiều trang.

## Khi có cả 3 cách áp dụng
Áp dụng cách gần nhất do code chạy từ trên xuống dưới, nên cách mới nhất sẽ áp dụng đè lên 2 cách còn lại.